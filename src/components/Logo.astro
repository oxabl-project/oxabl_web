---
interface Props {
  size?: "sm" | "lg"
  class?: string
}

const { size = "sm", class: className = "" } = Astro.props

// TODO: Replace the webfont wordmark with optically corrected SVG outlines.
---

<span
  class:list={[
    "inline-block shrink-0 font-wordmark text-ink lowercase",
    size === "lg" ? "text-heading" : "text-code",
    className,
  ]}
>
  oxabl
</span>
